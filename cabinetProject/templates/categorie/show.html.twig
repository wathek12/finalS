{% extends 'back.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ categorie.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_index') }}" class="btn btn-danger">back to list</a>

    <a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}" class="btn btn-default">edit</a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
